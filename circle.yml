# https://circleci.com/docs/config-sample
machine:
  ruby:
    version: 2.2.2

  services:
    - memcached

  # Add some environment variables
  environment:
    COVERAGE: true
    RACK_ENV: test
    CODECLIMATE_REPO_TOKEN: d7a640415d0028f0471153191bb4ab3e0f0fea34545e99f1c4f5532f7772f006

## Customize dependencies
#dependencies:

## Customize test commands
test:
  override:
    - bundle exec rspec

deployment:
  production:
    branch: master
    heroku:
      appname: app-stat-api
